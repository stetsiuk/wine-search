import { createSlice } from '@reduxjs/toolkit';

const initialState = {

}

export const WineSlice = createSlice({
	name: 'wine',
	initialState,
	reducers: {

	}
})

export const {} = WineSlice.actions;
export default WineSlice.reducer